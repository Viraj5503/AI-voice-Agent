# --- Core LLM ---
google-genai>=0.3.0           # Gemini 3 Flash via google-genai SDK
anthropic>=0.39.0             # juror bot only

# --- Voice ---
# Production voice loop with Gradium STT/TTS + native Google/Gemini LLM
# + Silero VAD.  Adds openai extra so we can swing the LLM hop to a local
# Ollama instance via its OpenAI-compatible /v1 endpoint when Gemini's
# free tier is throttled — see voice/livekit_agent.py:_build_llm.
# Pin span 1.4–1.x — current PyPI is 1.5.6.
livekit-agents[gradium,google,openai,silero]>=1.4,<2.0
# Fast laptop-mic prototyping (Person A H0–H4 step). Real PyPI tops out
# at 0.1.6; do NOT pin >=0.2.0 — that version doesn't exist:
gradbot>=0.1.5
# Direct streaming TTS for tools/scripts (current PyPI: 0.5.11):
gradium>=0.5.0

# --- Real-time tools ---
tavily-python>=0.5.0

# --- Extraction (Pioneer / Fastino bounty) ---
# current PyPI: gliner 0.2.26
gliner>=0.2.13
transformers>=4.45.0
torch>=2.4.0
huggingface-hub>=0.25.0

# Note on Python version: livekit-agents requires Python <3.15.  If you're
# on Python 3.14, everything in this file installs cleanly.  On 3.13 or 3.12
# you'll just get older livekit-agents 1.4.x — that's fine for our use.

# --- Bridge & dashboard ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=13.0

# --- Utility ---
python-dotenv>=1.0.1
pydantic>=2.9.0
rich>=13.8.0                  # pretty CLI for demo runner
httpx>=0.27.0
orjson>=3.10.0
