# --- Core LLM ---
google-genai>=0.3.0           # Gemini 3 Flash via google-genai SDK
anthropic>=0.39.0             # juror bot only

# --- Voice ---
# Production voice loop:
livekit-agents[gradium]~=1.4
# Fast prototyping (Person A H0–H4 step):
gradbot>=0.2.0
# Direct streaming TTS for tools/scripts:
gradium>=0.5.0

# --- Real-time tools ---
tavily-python>=0.5.0

# --- Extraction (Pioneer / Fastino bounty) ---
gliner>=0.2.13
transformers>=4.45.0
torch>=2.4.0
huggingface-hub>=0.25.0

# --- Bridge & dashboard ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=13.0

# --- Utility ---
python-dotenv>=1.0.1
pydantic>=2.9.0
rich>=13.8.0                  # pretty CLI for demo runner
httpx>=0.27.0
orjson>=3.10.0
